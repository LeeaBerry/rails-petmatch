

<h1>Your Bookings</h2>

<% if @bookings.any? %>
  <% @bookings.each do |booking| %>
    <div>
      <h3><%= booking.pet.pet_name %> (<%= booking.pet.pet_type %>)</h3>
      <p><strong>From:</strong> <%= booking.booking_startdate %></p>
      <p><strong>To:</strong> <%= booking.booking_enddate %></p>
    </div>
  <% end %>
<% else %>
  <p>You have no bookings yet.</p>
<% end %>
